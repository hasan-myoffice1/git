Git
Master branch
custom branch
local , remote 

 work Directory -> git add (staging Area) -> git commit (local Repository) ->git push (Remot Repository) 

git init

configaration (global)
git config --global user.name "name"
git config --global user.email "email.com"

single configaration
git config user.name "name"
git config user.email "@email.com"

check configration
git config --list

check status
git status 

local D. to stage 
git add filename ( git add demo.txt , git add "demo text.txt")
git add --all , git add . //all file


commit
git commit -m "add some text"

-----
git log 
git log --oneline

back to previus commit
git checkout number (git chackout 57e1559)

back git checkout master

how many change in last commit to present
git diff
git show ea14a95 

git rm filename //remove

git fatch and git pull



git clone link namgite
git clone https://github.com/hasan-myoffice1/git.git gitFile


git branch (branch list)
 git branch name (create new beanch)


git checkout new (switched present to onother branch)

git checkout -b home-brach 


 git stash and clean 

 .gitignore 

 fork in github  
 create pull request 




 


 
